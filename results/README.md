# To use displayGraph.py 
 - Install Matplotlib with the command ```$sudo apt-get install python-matplotlib ``` 
 - Run displayGraph.py with the command ```$python displayGraph.py```
The file values.txt contains the X values (for the moment)
